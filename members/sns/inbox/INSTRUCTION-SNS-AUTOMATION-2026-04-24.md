# 依頼: X / SNS まわりの自動化（優先）

| **依頼ID** | **SNS-AUTOMATION-2026-04-24** |
| **発行日** | 2026-04-24 |
| **発行者** | leader（経営者意向） |
| **回答期限** | 可能な範囲で早い段階で方針案 → 週次で差分OK |

## 目的

**SNS（主に X）の投稿・運用を、できる範囲で自動化する**こと。手作業のばらつきを減らし、`RUNBOOK` に沿った再現性を高める。

## やってほしいこと

1. **`members/sns/CLAUDE.md`** の「X 自動投稿」節を踏まえ、**いま正とする系統**（`tools/x_automation` vs iCloud `x-auto-post`）の整理方針を 1 枚にまとめる（未決なら**選択肢と推奨**を書く）。  
2. **`projects/x-automation/RUNBOOK.md`** を読み、**足りない手順・曖昧なところ**を追記 or 箇条書きで直す。  
3. 自動化に向けた**次の具体タスク 3 つ**（例: トピック JSON、Buffer 連携、エラーログの受け口）を、見積もり可能な形で列挙。  
4. **Buffer / スプレッドシート** 等、すでに動いているものがあれば**現状1パラグラフ**で要約（キーは書かない）。

## 置き場所

- 上記 1〜3 の内容は `members/sns/projects/` 以下に **Markdown 1 本**（例: `sns/projects/automation-plan-2026-04-24.md`）でよい。  
- **リーダー返却**（任意）: `members/leader/inbox/replies/SNS-AUTOMATION-2026-04-24-sns.md` に要約3行＋成果物パス。

## 禁止・注意

- API キー・トークンを本文に**書かない**（既存の `.env` 運用のまま）。  
- 釣り标题・誇大表現は既存の判断基準に従う。

## 合意

- 大きな方針変更（どちらの系統を正にするかの最終決定）が要る場合は **leader 経由で経営者確認**に回す。
