# 動画の指示（ここを書いて AI に渡す）

人間が書きやすいメモ用です。**最終的にレンダーに使うのは `video-prompt.json` だけ**です。  
このファイルを埋めたあと、Cursor などに「`video-prompt.json` の形式に変換して」と頼んでください。

## 今回の動画

- **全体の雰囲気・用途:**（例: YouTube 冒頭 10 秒の自己紹介）
- **合計の長さの目安:**（例: 10 秒）
- **解像度:**（省略時は 1920×1080）

## シーン 1

- **長さ（秒）:**
- **背景色:**（例: 青 #3B82F6）
- **テロップ:**（複数行なら行ごとに書く）

## シーン 2

- **長さ（秒）:**
- **背景色:**
- **テロップ:**

## シーン 3（必要なら追加）

- **長さ（秒）:**
- **背景色:**
- **テロップ:**

## メモ

- **背景画像・挿入画像:** ファイル名と、おおよその配置（全面 / ロゴだけ など）
- **BGM:** 雰囲気（明るい・落ち着いた等）と、全体にかけるかシーン単位か
- 実ファイルは `remotion/public/images/` ・ `remotion/public/audio/` に置き、AI に `video-prompt.json` のパスを書かせる（※権利は別途確認）
