# 週3回以上になりそうな作業 → 役職候補（ドラフト）

根拠: `~/Library/Mobile Documents/com~apple~CloudDocs/小野高誠のicloud/Cursorファイル/AI関連/Cursor/` 配下の**フォルダ構成・CLAUDE・運用設計**から推定。  
経営者の実カレンダーと突き合わせて、削る／統合する。

| 推定頻度 | 作業の内容（iCloud 上の根拠） | 役職候補（AI 社員名の例） |
|----------|--------------------------------|---------------------------|
| ほぼ毎日 | X（`@kousei_habit`）投稿の枠づくり・シート更新・Buffer 予約・画像付き prebuffer（`x-auto-post/CLAUDE.md`、launchd 系） | **SNS 担当社員** |
| 週複数 | 投稿文・画像プロンプトの生成・ストック管理（同上 + `generate_posts.py` 等） | **コンテンツ生成社員**（SNS と兼任可） |
| 週複数 | note / X 長文の執筆・リズム推敲（`面白いnote_X記事を作るためのプロジェクト…/note_article_project`） | **ライター社員**（既に `members/writer/` あり） |
| 週複数 | 学術班まわり：スライド・症例スケジュール・ガイド文書の生成・更新（`学術班/` のスクリプト・`schedule_case` 等） | **学術班オペレーション社員** |
| 週複数〜 | 習慣支援クライアント向け：ロードマップ・日報フォーマット・ToDo の更新（`my_cursor_workspace/proposal/clients/customer_b/`） | **カスタマーサクセス社員** |
| 状況次第 | Addness タスク確認（`addness-checker/check-tasks.js`） | **オペレーション社員**（兼任でよいことが多い） |
| 状況次第 | HP・採用 LP（`management/HP/`） | **Web／LP 社員**（更新が週3未満なら兼任） |
| 低〜中 | 動画まわり（`Video_auto/` は規模小さめ） | **動画担当社員**（週3に届くまで様子見） |
| 週複数（想定） | 案件探索・候補整理（Desktop `your-perfect-days/job-scout/`） | **リサーチャー社員** → `members/researcher/` |

## すでに `members/` にいる役

- `leader` … 朝 inbox・集約・レビュー
- `designer` … ビジュアル（X 画像などと相性よい）
- `writer` … 文章（note / メルマガと相性よい）
- `researcher` … 案件探索メモ・`job-scout` 連携

## 次の一手

1. 上表を紙かメモに写し、**「週3未満」と確信できる行を消す**  
2. 残った行ごとに `members/<英語スラッグ>/` を切るか、既存役に**吸収**するか決める  
3. 各フォルダに `CLAUDE.md` を置く（`~/ai-company/CLAUDE.md` の「メンバー（役職）の決め方」参照）

---

## 反映済み（2026-04-21）

次の社員フォルダを作成済み: `sns` / `academic` / `customer_success` / `operations` / `web` / `video` / `researcher`（各 `inbox/`・`projects/`・`CLAUDE.md`）。既存の `leader` / `writer` / `designer` に加えて `README.md` の一覧に記載。

## 参照パス（再確認用）

`~/Library/Mobile Documents/com~apple~CloudDocs/小野高誠のicloud/Cursorファイル/AI関連/Cursor/`

※ 案件探索 `job-scout` は **Desktop の `your-perfect-days/job-scout/`** 側。社員フォルダは **`members/researcher/`** を用意済み（`CLAUDE.md` にパス記載）。
